import { defineStyleConfig } from '@chakra-ui/react'

const baseStyle = {
  bg: 'gray.700',
  color: 'white',
  borderRadius: 'lg',
  p: 2
}

export const tooltipTheme = defineStyleConfig({ baseStyle })
